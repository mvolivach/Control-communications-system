<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Students</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <%- include('./partials/nav.ejs') %>
    <div class="container mt-5">
      <h1>Студенти:</h1>
      <ul class="list-unstyled">
        <% if (students.length) { %>
          <% students.forEach(({ _id, surname, createdAt, name, patronymic, phone, email, group, course, certifications, hasDueReminders, remindersDue }) => { %>
            <li>
              <div class="cont">
                <h5 class="card-title">
                  <a href="/students/<%= _id %>" class="text-decoration"><%= surname %> <%= name %> <%= patronymic %></a>
                </h5>           
                <p class="card-text"><strong>Номер телефону:</strong> <%= phone %></p>
                <p class="card-text"><strong>Email:</strong> <%= email %></p>
                <p class="card-text"><strong>Група:</strong> <%= group %></p>
                <p class="card-text"><strong>Курс:</strong> <%= course %></p>
                <p class="card-text"><strong>Кількість н/а за семестр:</strong> <%= certifications %></p>
                <div class="info"><span>Додано: <%= new Date(createdAt).toLocaleDateString() %></span></div>
                
                <div>
                  <button class="btn-delete btn btn-del" data-id="<%= _id %>">
                    Видалити
                  </button>
                  <button onclick="toggleArchive('<%= _id %>')" class="btn btn-del">Архівувати</button>
                  <% if (hasDueReminders) { %>
                    <div  role="alert">
                      <strong style="color: red;">Увага:</strong> Нагадувань, які потребують вашої уваги: <%= remindersDue.length %>.
                    </div>
                  <% } %>
                  <button class="btn btn-del" onclick="window.location.href='/students/<%= _id %>/reminders'">Переглянути нагадування</button>
                </div>
              </div>
            </li>
          <% }) %>
        <% } else { %>
          <li>Студентів не знайдено</li>
        <% } %>
      </ul>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      document.addEventListener('click', (e) => {
        const id = e.target?.dataset?.id || null;
        if (id && e.target.matches('.btn-delete, .btn-delete *')) {
          fetch(`/students/${id}`, {
            method: 'DELETE',
          }).then(() => {
            window.location.reload();
          }).catch((err) => {
            console.error('Помилка видалення студента:', err);
          });
        }
      });
      function toggleArchive(id) {
        fetch(`/students/toggle-archive/${id}`, {
          method: 'POST'
        }).then(() => {
          window.location.reload();
        }).catch(err => console.error('Error toggling archive status:', err));
      }
    </script>
  </body>
</html>
